# 视频号图书带货AI工作流系统产品需求文档（PRD）

**文档版本**：V1.0
**创建日期**：2026年06月04日
**文档状态**：正式发布
**产品负责人**：[待填写]
**技术负责人**：[待填写]
**设计负责人**：[待填写]

---

## ① 项目背景与目标

### 1.1 项目背景

基于"生财有术"社区成功案例复刻，原案例作者通过AI自动化工作流在23天内实现5万+佣金收入。本系统将文档中描述的7模块AI工作流产品化，帮助视频号图书带货创作者降本增效。

### 1.2 核心目标

| 目标维度 | 具体目标 | 衡量指标 | 目标值 |
|---------|---------|----------|--------|
| 成本目标 | 单条视频制作成本 | 实际成本 | ≤1.0元/条 |
| 效率目标 | 单条视频制作时间 | 端到端时间 | ≤15分钟/条 |
| 质量目标 | 平台审核通过率 | 用户反馈统计 | ≥95% |
| 规模目标 | 日处理能力 | 系统吞吐量 | ≥100条/日 |
| 用户目标 | 新手完成率 | 7天完成率 | ≥80% |

### 1.3 关键决策澄清

1. **技术选型**：使用 FFmpeg + MoviePy 实现视频合成
2. **用户模式**：单用户内部工具，用户自带 API Key，系统不垫付任何费用
3. **音频处理**：系统输出 TTS 分段文本（JSON），用户自行使用剪映等工具生成音频后上传
4. **字幕方案**：用户上传完整音频后，系统按字符比例自动对齐字幕；字幕为可选功能，默认开启
5. **资质要求**：系统仅负责内容生产，视频号小店账号、图书类目资质由用户自行解决

---

## ② 核心价值与业务目标

### 2.1 为用户解决的核心问题

| 问题类别 | 具体问题 | 本系统解决方案 | 用户价值 |
|---------|---------|----------------|----------|
| 制作门槛 | 视频制作复杂耗时 | 文本输入→自动生成 | 降低90%操作难度 |
| 制作成本 | 外包或自制成本高 | AI自动化降成本 | 成本从10-50元降至1元 |
| 合规风险 | 平台规则不熟悉 | 内置合规检查 | 降低违规封号风险 |
| 规模限制 | 手工制作难批量 | 批量处理能力 | 日产出提升10倍 |
| 内容质量 | 文案转化率低 | 专业提示词优化 | 提升点击和转化率 |

### 2.2 关键业务指标

| 指标层级 | 指标名称 | 计算公式 | 目标值 | 测量频率 |
|---------|---------|----------|--------|----------|
| 财务指标 | 单条视频成本 | 总成本/视频数 | ≤1.0元 | 实时 |
| 效率指标 | 处理成功率 | 成功数/总数 | ≥98% | 每日 |
| 质量指标 | 用户满意度 | 评分≥4分比例 | ≥90% | 每周 |
| 增长指标 | 用户活跃度 | 周活跃用户数 | ≥10人 | 每周 |
| 商业指标 | 用户产出价值 | 用户总佣金/成本 | ≥500% | 每月 |

### 2.3 成功标准

- **技术成功**：系统稳定运行30天，可用性 ≥99.5%
- **业务成功**：10个用户月均产出 ≥50条视频
- **财务成功**：用户平均ROI ≥500%
- **产品成功**：NPS净推荐值 ≥40

---

## ③ 用户场景与使用流程

### 3.1 目标用户画像

**核心用户：视频号图书带货新手**

- **身份**：个人创作者或小团队（1-3人）
- **经验**：有基础视频号操作经验，无批量制作能力
- **技术能力**：会用电脑，可能无编程经验
- **核心痛点**：想验证赛道但被制作复杂度劝退
- **内容方向**：健康养生类图书，50+女性受众

**扩展用户：规模化运营者**

- **身份**：已有成功案例，需要扩大规模
- **需求**：批量生产、成本控制、效率提升
- **使用频率**：每日10-100条视频

### 3.2 主要使用场景

**场景A：新手验证（低配版路径）**

用户故事：作为视频号新手，我想用最低成本验证图书带货是否可行，避免前期投入过大。

使用流程：
1. 进入低配版引导页面
2. 按照7个步骤操作，每步有免费工具推荐
3. 手动执行各环节（剪映、豆包等免费工具）
4. 产出第一条可发布视频

成功标准：40分钟内产出第一条视频，成本接近零。

**场景B：批量生产（高配版路径）**

用户故事：作为已验证模式的创作者，我需要批量生产高质量视频，控制成本在1元/条以内。

使用流程：
1. 批量上传多个ASR文本或ZIP压缩包
2. 选择所有7个模块，提交任务
3. 实时监控处理进度和预估成本
4. 处理完成后批量下载所有视频

成功标准：15分钟/条，1元/条，日处理100+条。

**场景C：爆款复制（专项需求）**

用户故事：作为有爆款视频的创作者，我需要最大化爆款价值，避免平台重复检测。

使用流程：
1. 输入已有爆款文案
2. 使用模块C进行智能去重
3. 生成3-5个不同版本
4. 分发到不同视频号账号

成功标准：去重后通过平台检测，转化率保持90%以上。

### 3.3 低配版路径（40分钟）

```
开始
 → 步骤1：提取逐字稿（推荐腾讯元宝，2分钟）
 → 步骤2：清洗正文（复制提示词A，粘贴文心一言，5分钟）
 → 步骤3：改写文案（复制提示词B，5分钟）
 → 步骤4：识别书籍（复制提示词D，3分钟）
 → 步骤5：生成配图（调用可灵/豆包，8分钟）
 → 步骤6：TTS分段+配音（复制提示词F，剪映配音，10分钟）
 → 步骤7：视频合成（剪映或CapCut，7分钟）
 → 完成，获得可发布视频
```

### 3.4 高配版路径（15分钟）

```
开始
 → 上传ASR文本（1分钟）
 → 选择模块，配置参数（1分钟）
 → 提交任务，系统自动处理（10分钟）
 → 上传用户生成的TTS音频（1分钟）
 → 系统自动合成视频（2分钟）
 → 下载MP4文件
 → 完成
```

---

## ④ 功能范围与系统边界

### 4.1 功能模块总览

| 模块 | 功能名称 | 核心功能描述 | 输入 | 输出 | 必选/可选 | 依赖模块 |
|------|---------|-------------|------|------|-----------|----------|
| A | 逐字稿清洗 | 修复ASR错字，去除营销噪声，合规过滤 | 原始ASR文本 | 清洗后纯正文 | 必选 | — |
| B | 口播文案改写 | 轻量改写，保留爆点，50+女性受众优化 | 清洗后文案 | 口播文案 | 必选 | A |
| C | 轻量去重微调 | 爆款文案去重，避免平台重复检测 | 口播文案 | 去重后文案 | 可选 | B |
| D | 书名作者识别 | 从文案中提取书籍信息，结构化输出 | 口播文案 | JSON书籍信息 | 可选 | B |
| E | 配图生成 | 调用绘图API生成3-6张配图 | 书籍信息+关键词 | 图片文件 | 必选 | D |
| F | TTS段落切分 | 将文案切分为适合TTS朗读的段落 | 口播文案 | JSON分段文本 | 必选 | B |
| G | 视频合成 | 将图片+音频+字幕合成为MP4视频 | 图片+音频+字幕 | MP4视频文件 | 必选 | E, F |
| H | 合规闸门 | 文案定稿后发布前的三层合规检查 | 口播文案 | 通过/拦截+违规项 | 系统强制 | B（或C） |

> 说明：A-G 为面向用户的 7 个工作流模块（`modules` 字段可选范围）；H 是系统在文案定稿后自动插入的强制合规闸门，不在用户可选模块内，用户不可关闭。详见第 ⑩ 章。

### 4.2 模块A：逐字稿清洗

**功能描述**：将原始ASR文本清洗为可读纯正文，修复错别字，去除营销噪声，过滤平台违规表达。

**完整提示词**：

```
你是逐字稿修复清洗助手。你需要在保留事实和原文顺序的前提下，删除非正文噪声，修复乱码和明显ASR同音错词。你必须同时遵守视频号内容安全要求，避免输出任何低俗、暴力、虚假夸大、医疗承诺、导流诱导或误导性表达。输出必须是清洗后的纯正文。

请对下面的逐字稿做修复型清洗。

你可以做的事：
1. 删除明显属于原博主的栏目口号、作者自称、互动引导、导流主页、平台水印、往期节目提示等非正文钩子。
2. 删除跨平台搬运水印，例如"优优独播剧场——YoYo Television Series Exclusive""谢谢观看"。
3. 删除明显重复拼接的段落，只保留一次。
4. 对乱码符号和明显ASR同音错词做上下文推测修复，例如"轻运"应结合语境修成"清运"，"飘铃/飘龄"应结合尿酸语境修成"嘌呤"。
5. 适度补充必要标点，让正文可读。
6. 进一步删除或改写视频号高风险表达：低俗擦边、血腥暴力、虚假夸大、医疗承诺、诱导互动、导流私信/评论区/主页、恐吓式逼单、伪装权威结论。

严禁做的事：
1. 不要改写观点、人物、时间、数字、案例和核心事实。
2. 不要概括、扩写、重排正文结构。
3. 不要输出标题、解释、Markdown、修改说明。

如果不确定某个词该怎么修，就保留原词，不要编造新信息。

主题关键词：{keyword}
原视频标题：{title}
原作者标识：{author}

请基于下面的原始逐字稿，返回修复清洗后的正文：
{transcript}
```

**接口规范**：

端点：`POST /api/v1/module-a/clean`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| transcript | string | 是 | 原始ASR文本，10-5000字符 |
| keyword | string | 否 | 主题关键词 |
| title | string | 否 | 原视频标题 |
| author | string | 否 | 原作者标识 |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| cleaned_text | string | 清洗后正文 |
| cost | float | 本次调用成本（元） |
| processing_time | int | 处理时间（秒） |
| changes_made | array | 修改记录列表 |

状态码：`200` 成功 / `400` 参数错误 / `429` 频率限制 / `500` 处理错误

**业务规则**：必须保留原文核心事实和顺序；必须删除所有营销话术和水印；必须修复明显ASR同音错字；必须符合视频号内容安全要求。

### 4.3 模块B：口播文案改写

**功能描述**：将清洗后的正文改写成适合50+女性受众的口播文案，保留爆点，优化表达。

**完整提示词**：

```
你是视频号图书带货口播文案专家，专门为50+女性受众优化健康类图书推荐文案。

你的任务是将清洗后的正文改写成适合视频号口播的版本，需要同时满足：
1. 转化效果：促进图书购买
2. 平台合规：通过视频号内容审核
3. 受众喜爱：符合50+女性观看偏好

文案改写要求：

【开头钩子（3秒内）】
- 使用疑问句或惊人事实开场，引发健康关注或好奇心
- 例如："你知道吗？很多中老年人的健康问题其实可以提前预防..."

【中间内容】
- 保留原文核心健康知识点，将医学术语转化为通俗表达
- 加入具体案例或故事（如"我家里长辈用了这个方法后..."）
- 突出书籍的实用价值：每天几分钟、简单易行
- 强调情感价值：为家人健康负责、让自己更安心

【结尾行动号召】
- 温和引导，避免强硬推销
- 例如："这本书现在有活动，点击下方链接了解一下，为了家人健康值得看看"

【风格要求】
- 语气：亲切如朋友推荐，有参考价值但不傲慢
- 用词：避免网络流行语，使用"咱们""您"等尊称
- 长度：适合1-2分钟口播（200-400字）

【合规注意】
- 不用"治愈""根治""100%有效"等绝对化表达
- 用"分享""参考"代替"建议""治疗"
- 不制造过度健康焦虑
- 符合视频号带货规范

目标受众：{target_audience}
原视频标题：{title}

请基于以下清洗后的正文，改写成口播文案：
{cleaned_text}
```

**接口规范**：

端点：`POST /api/v1/module-b/rewrite`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cleaned_text | string | 是 | 清洗后正文 |
| target_audience | string | 是 | 目标受众，默认"50+女性" |
| title | string | 否 | 原视频标题 |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| script | string | 口播文案 |
| hooks | array | 钩子位置列表 |
| call_to_action | string | 结尾行动号召文案 |
| estimated_duration | int | 预估朗读时长（秒） |
| cost | float | 本次调用成本（元） |

状态码：`200` 成功 / `400` 参数错误 / `429` 频率限制 / `500` 处理错误

### 4.4 模块C：轻量去重微调

**功能描述**：对爆款文案进行去重处理，使其能够二次发布而不被平台检测为重复内容。

**完整提示词**：

```
你是文案去重优化专家，专门处理视频号爆款文案的二次发布优化。

你的任务是对已有爆款口播文案进行去重处理，使其能够：
1. 通过平台重复内容检测
2. 保持原爆款的核心转化力
3. 适应不同发布时段的观众偏好

去重优化策略：

【结构重组】
- 调整开头钩子的表达方式（疑问句↔陈述句↔故事开场）
- 重新组织中间内容的讲述顺序
- 改变案例的呈现方式（第一人称↔第三人称）

【同义词替换】
- 替换20-30%的关键词，使用同义表达
- 调整程度副词："非常"→"特别"、"完全"→"基本"

【表达方式调整】
- 改变句式结构（长句拆分、短句合并）
- 增加或减少情感修饰词

【保持核心】
- 必须保留原爆款的：核心知识点、转化钩子、行动号召
- 保持相同的目标受众定位和情感基调

【输出要求】
- 去重后文案长度变化不超过±10%
- 可读性不低于原文
- 平台违规风险不高于原文

去重强度：{deduplication_level}（低/中/高）

请对以下爆款文案进行去重优化，直接输出优化后的文案，不要输出解释：
{original_script}
```

**接口规范**：

端点：`POST /api/v1/module-c/deduplicate`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| original_script | string | 是 | 原爆款文案 |
| deduplication_level | string | 否 | 去重强度：低/中/高，默认中 |
| preserve_hooks | boolean | 否 | 是否保留钩子，默认 true |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| optimized_script | string | 去重后文案 |
| similarity_score | float | 与原文案相似度（0-1） |
| changes_made | array | 修改项列表 |
| estimated_pass_rate | float | 预估平台通过率 |
| cost | float | 本次调用成本（元） |

状态码：`200` 成功 / `400` 参数错误 / `429` 频率限制 / `500` 处理错误

**业务规则**：相似度控制在30%-70%之间；核心爆点和转化钩子必须保留；语义和情感基调不能改变。

### 4.5 模块D：书名作者识别

**功能描述**：从口播文案中自动识别提到的书籍名称和作者信息，输出结构化JSON。

**完整提示词**：

```
你是图书信息识别专家，专门从健康类口播文案中提取书籍信息。

识别规则：
- 直接提及：文案中明确出现的书名（带《》或不带）
- 作者识别：文案中出现的作者姓名或专家背书
- 置信度评估：
  - 高置信度（0.8-1.0）：明确提及书名和作者
  - 中置信度（0.5-0.8）：只提及书名或只提及作者
  - 低置信度（0.2-0.5）：通过内容推断可能书籍

健康图书分类参考：
- 慢性病管理：糖尿病、高血压、心脑血管
- 养生保健：中医养生、食疗、运动康复
- 心理健康：情绪管理、睡眠改善、压力缓解
- 家庭护理：老人照护、急救知识、居家安全

最低置信度要求：{min_confidence}

请从以下口播文案中识别书籍信息，严格按JSON格式输出，不要输出其他内容：
{"books": [{"title": "书名", "author": "作者", "confidence": 0.95, "extracted_from": "原文片段", "category": "分类", "reasoning": "识别理由"}]}

口播文案：
{script_text}
```

**接口规范**：

端点：`POST /api/v1/module-d/identify-books`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| script_text | string | 是 | 口播文案 |
| category_hint | string | 否 | 分类提示，如"健康养生" |
| min_confidence | float | 否 | 最低置信度，默认0.5 |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| books | array | 识别结果列表 |
| books[].title | string | 书名 |
| books[].author | string | 作者 |
| books[].confidence | float | 置信度（0-1） |
| books[].extracted_from | string | 原文片段 |
| books[].category | string | 健康分类 |
| total_identified | int | 识别总数 |

状态码：`200` 成功 / `400` 参数错误 / `500` 处理错误

**业务规则**：置信度≥0.8可直接使用；置信度0.5-0.8需用户确认；必须提供原文引用。

### 4.6 模块E：配图生成

**功能描述**：根据书籍信息和文案内容，调用绘图API生成视频所需的3-6张配图。

**配图规格**：

| 图片类型 | 数量 | 显示时机 | 停留时长 | Prompt重点 |
|---------|------|---------|---------|------------|
| 封面图 | 1张（必须） | 视频0-3秒 | 3-5秒 | 书名清晰+健康主题+吸引点击 |
| 内容插图 | 2-4张 | 中间段落 | 8-15秒/张 | 知识可视化+情感共鸣 |
| 结尾CTA图 | 1张（必须） | 最后5-10秒 | 5-8秒 | 行动指引+价值强调 |

**图片生成Prompt模板**：

封面图：`《{book_title}》书籍封面风格，健康主题，{health_topic}相关元素，柔和暖色调，适合中老年女性观看，温暖亲切，清晰易读，竖版1080x1920`

内容插图：`{content_description}场景，健康生活方式，温馨家庭氛围，50+女性形象，柔和色调，知识可视化，简洁清晰，竖版1080x1920`

结尾CTA图：`《{book_title}》书籍特写，温和号召性色调，"立即了解"指引元素，价值突出，适合购买决策场景，竖版1080x1920`

**接口规范**：

端点：`POST /api/v1/module-e/generate-images`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| book_info | object | 是 | 书籍信息（title/author/category） |
| script_segments | array | 是 | 文案分段，用于匹配内容插图 |
| image_count | int | 否 | 图片数量，默认5，范围3-6 |
| style_preference | string | 否 | 风格偏好，默认"温暖亲切" |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| images | array | 图片列表 |
| images[].url | string | 图片URL |
| images[].type | string | 图片类型（cover/content/cta） |
| images[].suggested_duration | int | 建议显示时长（秒） |
| total_cost | float | 总成本（元） |

状态码：`200` 成功 / `400` 参数错误 / `402` 成本超限 / `500` 生成错误

**业务规则**：必须生成3-6张图片（1封面+1-4内容+1结尾）；所有图片必须为1080x1920竖版；图片内容必须符合平台规范。

### 4.7 模块F：TTS段落切分

**功能描述**：将口播文案切分为适合TTS朗读的段落，每段约26秒，供用户用剪映等工具生成音频。

**完整提示词**：

```
你是中文短视频配音文案拆段助手。
你的唯一任务是把给定文案按原顺序拆成多个适合TTS的自然段。
严禁改写、增删、概括、润色或重排内容。
输出必须是严格JSON：{"segments": ["...", "..."]}
每段尽量控制在24到28秒内，绝不能故意合并成长段。
如果原文本来很短，也至少返回一个segment。
不要输出markdown，不要输出解释。

主题关键词：{keyword}
原视频标题：{title}
目标单段时长：26秒以内

请基于下面这段候选稿（最终配音文案）拆段：
{script_text}
```

**接口规范**：

端点：`POST /api/v1/module-f/split-tts`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| script_text | string | 是 | 口播文案 |
| keyword | string | 否 | 主题关键词 |
| title | string | 否 | 视频标题 |
| target_segment_duration | int | 否 | 目标单段时长（秒），默认26 |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| segments | array | 分段列表 |
| segments[].text | string | 分段文本 |
| segments[].estimated_duration | int | 预估时长（秒） |
| segments[].start_index | int | 原文起始位置 |
| total_duration | int | 总预估时长（秒） |
| segment_count | int | 分段数量 |

状态码：`200` 成功 / `400` 参数错误 / `500` 处理错误

**业务规则**：每段必须在24-28秒之间；必须在自然停顿处切分；输出必须是严格JSON格式。

### 4.8 模块G：视频合成

**功能描述**：使用 FFmpeg + MoviePy 将用户上传的音频、系统生成的图片和字幕合成为完整MP4视频。

**字幕对齐算法**：用户上传一整段完整音频，系统按字符比例自动分配字幕时间：

```
字幕时间 = (分段文本位置 / 总文本长度) × 音频总时长
```

具体实现步骤：
1. 读取音频总时长 `T_total`
2. 计算所有分段文本总字符数 `C_total`
3. 对每个分段 i，其字符数为 `C_i`，累计起始字符位置为 `C_start_i`
4. 该分段字幕开始时间 = `(C_start_i / C_total) × T_total`
5. 该分段字幕结束时间 = `((C_start_i + C_i) / C_total) × T_total`

**视频合成流程**：

```
1. 校验输入（图片数量、音频格式、分段文本）
2. 图片预处理（统一缩放至1080x1920，居中裁剪）
3. 计算每张图片显示时长（按音频总时长 / 图片数量分配，或按分段时长加权）
4. 应用Ken Burns动效（缓慢缩放/平移，可选）
5. 生成字幕轨道（按字符比例对齐算法）
6. 合成视频轨道（图片序列 + 转场）
7. 混合音频轨道
8. 叠加字幕（可选）
9. 编码输出MP4（H.264 + AAC）
```

**接口规范**：

端点：`POST /api/v1/module-g/compose-video`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| task_id | string | 是 | 任务ID，关联图片和分段文本 |
| audio_url | string | 是 | 用户上传的音频文件URL |
| enable_subtitles | boolean | 否 | 是否启用字幕，默认 true |
| enable_animations | boolean | 否 | 是否启用Ken Burns等动效，默认 true |
| output_config | object | 否 | 输出配置（分辨率/帧率/格式） |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| video_url | string | 视频文件URL |
| duration | int | 视频时长（秒） |
| file_size | int | 文件大小（字节） |
| render_time | int | 渲染时间（秒） |

状态码：`200` 成功 / `400` 参数错误 / `500` 渲染错误

**业务规则**：输出分辨率必须为1080x1920；帧率必须≥25fps；音画同步偏差≤0.5秒；视频时长30-300秒。

### 4.9 辅助接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/tasks` | POST | 创建新任务 |
| `/api/v1/tasks/{task_id}` | GET | 查询任务状态 |
| `/api/v1/tasks/{task_id}/cancel` | POST | 取消任务 |
| `/api/v1/tasks/batch` | POST | 批量创建任务 |
| `/api/v1/tasks/{task_id}/download` | GET | 下载任务结果 |
| `/api/v1/config` | GET | 获取当前配置 |
| `/api/v1/config` | PUT | 更新配置 |
| `/api/v1/config/test-api` | POST | 测试API Key连通性 |
| `/api/v1/guide/steps` | GET | 获取7步骤引导内容 |
| `/api/v1/guide/prompts/{module}` | GET | 获取指定模块提示词 |

### 4.10 系统边界

**系统内部处理（本系统负责）**：

- AI文案处理（模块A-D）
- 图片生成调度（模块E）
- 视频合成渲染（模块G）
- 任务管理与成本监控
- 用户配置管理

**用户手动操作（本系统不负责）**：

- 提取原始ASR文本（腾讯元宝等工具）
- 生成TTS音频（剪映、Azure、阿里云等）
- 上传音频文件到系统
- 发布视频到视频号平台
- 添加商品链接和日常运营

### 4.11 非功能需求

| 类别 | 指标 | 要求 | 测量方式 |
|------|------|------|----------|
| 性能 | 单条处理时间 | ≤15分钟 | 端到端计时 |
| 性能 | API响应时间 | P95≤2秒 | API监控 |
| 性能 | 系统可用性 | ≥99.5% | 监控系统 |
| 安全 | API Key存储 | 加密存储 | 代码审查 |
| 安全 | 数据传输 | HTTPS | 网络检查 |
| 兼容 | 浏览器支持 | Chrome 90+, Safari 14+ | 测试验证 |
| 兼容 | 音频格式 | MP3, WAV, M4A | 测试验证 |

---

## ⑤ 字段规则表

### 5.1 任务字段规则

| 字段名 | 类型 | 必填 | 默认值 | 取值范围 | 校验规则 | 备注 |
|--------|------|------|--------|----------|----------|------|
| task_id | string | 系统生成 | UUID | — | 全局唯一 | 主键 |
| user_id | string | 系统生成 | 固定值 | — | — | 单用户模式固定 |
| status | enum | 系统维护 | pending | pending/processing/blocked/completed/failed/cancelled | 状态流转校验 | blocked=合规拦截待处理 |
| transcript | string | 是 | — | 10-5000字符 | 非空 | 原始ASR输入 |
| modules | array | 是 | ["A","B","E","F","G"] | A-G子集 | 至少含A、B、G | 模块选择 |
| cost_limit | float | 是 | 1.0 | 0.1–5.0 | 小数点后2位 | 元为单位 |
| time_limit | int | 是 | 900 | 60–3600 | 正整数 | 秒，超时自动停 |
| target_audience | enum | 是 | 50+女性 | 50+女性/健康关注者/家庭主妇/通用 | 枚举校验 | 受众定向 |
| output_format | enum | 是 | mp4 | mp4/mov | 枚举校验 | 视频格式 |
| output_resolution | enum | 是 | 1080x1920 | 1080x1920/720x1280 | 必须9:16 | 分辨率 |
| enable_subtitles | boolean | 是 | true | true/false | — | 是否生成字幕 |
| enable_animations | boolean | 是 | true | true/false | — | 是否启用动效 |
| created_at | datetime | 系统生成 | 当前时间 | ISO8601 | — | 创建时间 |
| updated_at | datetime | 系统维护 | 当前时间 | ISO8601 | — | 更新时间 |

### 5.2 配置字段规则

| 字段名 | 类型 | 必填 | 默认值 | 校验规则 | 备注 |
|--------|------|------|--------|----------|------|
| llm_api_key | string | 是 | — | 加密存储，非空 | 大模型API密钥 |
| llm_provider | enum | 是 | deepseek | deepseek/openai/qwen/doubao | LLM供应商 |
| llm_model | string | 是 | deepseek-chat | 非空 | 模型名称 |
| image_api_key | string | 是 | — | 加密存储，非空 | 绘图API密钥 |
| image_provider | enum | 是 | doubao | doubao/kling/tongyi | 绘图供应商 |
| daily_cost_cap | float | 否 | 100.0 | ≥0 | 每日成本上限（元） |
| concurrency | int | 否 | 3 | 1–10 | 并发任务数 |

### 5.3 状态流转规则

```
pending（待处理）
  → processing（处理中）      [提交任务]
  → cancelled（已取消）        [用户取消]

processing（处理中）
  → completed（已完成）        [全部模块成功]
  → blocked（合规拦截）        [模块H未通过]
  → failed（失败）             [任一必选模块失败 / 超时 / 超成本]
  → cancelled（已取消）        [用户取消]

blocked（合规拦截，待处理）
  → processing（处理中）        [用户修改文案后重跑]
  → cancelled（已取消）         [用户放弃]

failed / completed / cancelled 为终态，不可再流转
blocked 为可恢复态，修改文案后可重新进入 processing
```

### 5.4 校验错误码

| 错误码 | 含义 | 处理建议 |
|--------|------|----------|
| E1001 | transcript长度不足 | 提示至少10字符 |
| E1002 | transcript超长 | 提示最多5000字符 |
| E1003 | modules缺少必选项 | 提示必须包含A、B、G |
| E1004 | cost_limit超范围 | 提示0.1-5.0之间 |
| E2001 | API Key无效 | 提示检查配置 |
| E2002 | API Key余额不足 | 提示充值 |
| E3001 | 音频格式不支持 | 提示MP3/WAV/M4A |
| E3002 | 音频时长异常 | 提示30-300秒 |

---

## ⑥ 交互与界面说明

### 6.1 页面结构总览

| 页面 | 路径 | 核心功能 | 目标用户 |
|------|------|----------|----------|
| 首页 | / | 模式选择（低配版/高配版） | 全部 |
| 低配版引导 | /guide | 7步骤手动操作引导 | 新手 |
| 任务创建 | /tasks/new | 上传文本，配置模块 | 进阶用户 |
| 批量任务 | /tasks/batch | 批量上传，批量处理 | 规模用户 |
| 任务详情 | /tasks/:id | 进度监控，结果预览 | 全部 |
| 音频上传 | /tasks/:id/audio | 上传TTS音频 | 全部 |
| 任务列表 | /tasks | 历史任务管理 | 全部 |
| 配置中心 | /config | API Key、供应商配置 | 全部 |

### 6.2 低配版引导页交互

```
┌─────────────────────────────────────┐
│  视频号图书带货 · 低配版引导          │
├─────────────────────────────────────┤
│  进度： ●─●─○─○─○─○─○   (2/7)        │
│                                      │
│  步骤2：清洗逐字稿正文                │
│  ┌─────────────────────────────┐    │
│  │ 推荐工具：文心一言（免费）     │    │
│  │ [打开文心一言 ↗]              │    │
│  └─────────────────────────────┘    │
│                                      │
│  提示词（点击复制）：                 │
│  ┌─────────────────────────────┐    │
│  │ 你是逐字稿修复清洗助手...      │    │
│  │                    [复制 📋]  │    │
│  └─────────────────────────────┘    │
│                                      │
│  [上一步]            [下一步 →]      │
└─────────────────────────────────────┘
```

交互要点：
- 每步骤显示推荐免费工具及跳转链接
- 提示词一键复制，复制成功有 Toast 反馈
- 进度条可点击跳转任意已完成步骤
- 步骤间数据可暂存到 localStorage，刷新不丢失

### 6.3 高配版任务创建页交互

```
┌─────────────────────────────────────┐
│  创建任务                            │
├─────────────────────────────────────┤
│  逐字稿文本                          │
│  ┌─────────────────────────────┐    │
│  │ [粘贴或拖入文本...]           │    │
│  │                              │    │
│  └─────────────────────────────┘    │
│  字数：1234 / 5000                   │
│                                      │
│  处理模块（拖动排序）                 │
│  [✓A清洗] [✓B改写] [□C去重]         │
│  [✓D识别] [✓E配图] [✓F分段] [✓G合成]│
│                                      │
│  受众：[50+女性 ▼]  成本上限：[1.0元]│
│  字幕：[✓]  动效：[✓]                │
│                                      │
│  预估成本：约 0.85 元  预估时长：12分 │
│  [提交任务]                          │
└─────────────────────────────────────┘
```

交互要点：
- 模块依赖自动校验（如选E必须选D，选G必须有E、F）
- 实时预估成本，超过上限红色提示
- 必选模块（A/B/G）默认勾选且不可取消

### 6.4 任务详情页交互

```
┌─────────────────────────────────────┐
│  任务 task_abc123        [处理中...]  │
├─────────────────────────────────────┤
│  A清洗 ✓  B改写 ✓  E配图 ⟳  F分段 ○ │
│  G合成 ○                             │
│                                      │
│  当前：模块E 配图生成中 (3/5)         │
│  ▓▓▓▓▓▓▓░░░  68%                     │
│                                      │
│  已用成本：0.42元 / 1.0元            │
│  已用时间：4分12秒 / 15分            │
│                                      │
│  [取消任务]                          │
└─────────────────────────────────────┘
```

交互要点：
- 模块状态实时更新（轮询或 WebSocket）
- 配图完成后可预览缩略图
- 文案模块完成后可展开查看文本
- 成本/时间接近上限时变橙色预警

### 6.5 音频上传页交互

```
┌─────────────────────────────────────┐
│  上传配音音频                         │
├─────────────────────────────────────┤
│  系统已生成 5 段配音文案：            │
│  ┌─────────────────────────────┐    │
│  │ 段1：你知道吗，很多中老年...   │    │
│  │ 段2：其实这本书里讲到...       │    │
│  │ ...                  [复制全部]│    │
│  └─────────────────────────────┘    │
│                                      │
│  请用剪映/Azure等生成音频后上传：     │
│  ┌─────────────────────────────┐    │
│  │   拖入音频文件 或 [选择文件]   │    │
│  │   支持 MP3/WAV/M4A            │    │
│  └─────────────────────────────┘    │
│                                      │
│  [上传并开始合成]                     │
└─────────────────────────────────────┘
```

交互要点：
- 分段文案一键复制，方便用户去TTS工具生成
- 上传后校验格式和时长
- 字幕将按字符比例自动对齐，无需用户操作

### 6.6 关键交互反馈规则

| 场景 | 反馈方式 | 内容 |
|------|----------|------|
| 复制提示词 | Toast | "已复制到剪贴板" |
| 提交任务成功 | Toast + 跳转 | "任务已创建，正在处理" |
| 成本超限 | Modal阻断 | "预估成本超过上限，请调整" |
| 模块依赖缺失 | 内联红字 | "选择配图需先开启书籍识别" |
| 音频格式错误 | 内联红字 | "仅支持MP3/WAV/M4A格式" |
| 任务失败 | Banner | "处理失败：[原因]，[重试]" |
| API Key无效 | Modal | "API Key验证失败，请前往配置" |

---

## ⑦ 技术架构与实现要点

### 7.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | React + TypeScript + Vite | 单页应用 |
| 前端UI | TailwindCSS + shadcn/ui | 组件库 |
| 后端 | Python + FastAPI | API服务 |
| 任务队列 | Celery + Redis | 异步任务处理 |
| 视频合成 | FFmpeg + MoviePy | 视频渲染 |
| 数据库 | SQLite / PostgreSQL | 任务与配置存储 |
| 文件存储 | 本地文件系统 / 对象存储 | 图片、音频、视频 |

### 7.2 系统架构图

```
┌──────────────┐
│   前端 (React) │
└──────┬───────┘
       │ HTTPS / REST
┌──────▼───────────────────────────┐
│        FastAPI 网关层              │
│  路由 / 鉴权 / 参数校验 / 限流      │
└──────┬───────────────────────────┘
       │
┌──────▼──────┐    ┌──────────────┐
│ 任务管理服务 │───▶│ Celery 任务队列│
└──────┬──────┘    └──────┬───────┘
       │                  │
       │         ┌────────▼─────────┐
       │         │  Worker 处理池    │
       │         │ ┌──────────────┐ │
       │         │ │ A-D: LLM调用  │ │
       │         │ │ E: 绘图API调用 │ │
       │         │ │ F: 分段处理   │ │
       │         │ │ G: FFmpeg合成 │ │
       │         │ └──────────────┘ │
       │         └────────┬─────────┘
┌──────▼──────┐    ┌──────▼───────┐
│   数据库     │    │   文件存储    │
│ 任务/配置    │    │ 图/音/视频    │
└─────────────┘    └──────────────┘
```

### 7.3 成本核算模型

| 模块 | 调用类型 | 单次成本估算 | 说明 |
|------|----------|-------------|------|
| A 清洗 | LLM | 约0.01-0.03元 | 按token，DeepSeek低价 |
| B 改写 | LLM | 约0.01-0.03元 | 同上 |
| C 去重 | LLM | 约0.01-0.03元 | 可选 |
| D 识别 | LLM | 约0.005元 | 输出短 |
| E 配图 | 绘图API | 约0.1-0.5元 | 5张，主要成本 |
| F 分段 | LLM | 约0.005元 | 输出JSON |
| G 合成 | 本地计算 | 约0元 | 仅算力 |
| **合计** | — | **约0.15-0.6元** | 控制在1元内 |

成本控制策略：
- 任务级成本上限，超限自动停止
- 每日成本上限，保护用户账户
- 配图使用低价供应商（豆包等）
- LLM优先 DeepSeek 等高性价比模型

### 7.4 关键实现要点

**1. API Key 安全**
- 使用对称加密（如 Fernet）存储于数据库
- 内存中解密使用，不落明文日志
- 前端仅显示掩码（如 `sk-****1234`）

**2. 任务幂等性**
- 每个模块结果持久化，失败可从断点重试
- 重试不重复扣费已完成模块

**3. 成本实时计算**
- 每次API调用后立即记录token和费用
- 超过上限时中断任务，保留已完成结果

**4. 视频合成性能**
- FFmpeg 硬件加速（如可用）
- 图片预处理与音频解析并行
- 临时文件及时清理

---

## ⑧ 数据模型与存储设计

### 8.1 数据库表设计

**tasks（任务主表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | varchar(40) | PK | 任务ID（task_xxx） |
| user_id | varchar(40) | NOT NULL | 用户ID（单用户固定） |
| status | varchar(20) | NOT NULL, default 'pending' | 状态枚举 |
| transcript | text | NOT NULL | 原始ASR文本 |
| keyword | varchar(100) | NULL | 主题关键词 |
| title | varchar(200) | NULL | 原视频标题 |
| author | varchar(100) | NULL | 原作者标识 |
| modules | json | NOT NULL | 启用模块数组 |
| target_audience | varchar(30) | NOT NULL | 受众定向 |
| cost_limit | decimal(6,2) | NOT NULL | 成本上限（元） |
| time_limit | int | NOT NULL | 时间上限（秒） |
| enable_subtitles | boolean | default true | 是否字幕 |
| enable_animations | boolean | default true | 是否动效 |
| total_cost | decimal(8,4) | default 0 | 累计成本 |
| error_code | varchar(20) | NULL | 失败错误码 |
| error_message | text | NULL | 失败原因 |
| batch_id | varchar(40) | NULL, FK | 所属批次 |
| created_at | datetime | NOT NULL | 创建时间 |
| updated_at | datetime | NOT NULL | 更新时间 |

索引：`idx_status(status)`、`idx_batch(batch_id)`、`idx_created(created_at)`

**module_results（模块结果表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK, auto | 自增主键 |
| task_id | varchar(40) | NOT NULL, FK | 所属任务 |
| module | char(1) | NOT NULL | 模块标识 A-G |
| status | varchar(20) | NOT NULL | pending/success/failed |
| input_ref | text | NULL | 输入引用（前序产物） |
| output | json | NULL | 模块输出（文本/JSON/URL列表） |
| cost | decimal(8,4) | default 0 | 本模块成本 |
| tokens_in | int | NULL | 输入token数 |
| tokens_out | int | NULL | 输出token数 |
| retry_count | int | default 0 | 已重试次数 |
| started_at | datetime | NULL | 开始时间 |
| finished_at | datetime | NULL | 完成时间 |

唯一约束：`uniq(task_id, module)` — 每任务每模块一条，重试覆盖更新。

**assets（产物文件表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | varchar(40) | PK | 资产ID |
| task_id | varchar(40) | NOT NULL, FK | 所属任务 |
| type | varchar(20) | NOT NULL | image/audio/video |
| sub_type | varchar(20) | NULL | cover/content/cta（图片用） |
| file_path | varchar(500) | NOT NULL | 存储路径 |
| file_size | bigint | NULL | 字节数 |
| mime_type | varchar(50) | NULL | MIME类型 |
| meta | json | NULL | 时长/分辨率等元数据 |
| expires_at | datetime | NULL | 过期时间 |
| created_at | datetime | NOT NULL | 创建时间 |

**configs（配置表，单行）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | int | PK | 固定为1 |
| llm_provider | varchar(20) | NOT NULL | LLM供应商 |
| llm_model | varchar(50) | NOT NULL | 模型名 |
| llm_api_key_enc | varbinary | NOT NULL | 加密的LLM密钥 |
| image_provider | varchar(20) | NOT NULL | 绘图供应商 |
| image_api_key_enc | varbinary | NOT NULL | 加密的绘图密钥 |
| daily_cost_cap | decimal(8,2) | default 100 | 每日成本上限 |
| concurrency | int | default 3 | 并发数 |
| updated_at | datetime | NOT NULL | 更新时间 |

**cost_logs（成本流水表）**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK, auto | 自增主键 |
| task_id | varchar(40) | NOT NULL | 任务ID |
| module | char(1) | NOT NULL | 模块 |
| provider | varchar(20) | NOT NULL | 供应商 |
| cost | decimal(8,4) | NOT NULL | 成本 |
| created_at | datetime | NOT NULL | 时间（用于日聚合） |

索引：`idx_day(created_at)` 用于每日成本上限聚合查询。

### 8.2 文件存储生命周期

**存储目录结构**

```
storage/
  {task_id}/
    images/     封面、内容、CTA图片（PNG/JPG）
    audio/      用户上传的配音音频
    video/      合成后的MP4成品
    temp/       渲染中间产物（合成后清理）
```

**生命周期规则**

| 文件类型 | 默认保留期 | 过期处理 | 大小上限 |
|---------|-----------|----------|----------|
| 图片 | 7天 | 自动删除 | 单张≤5MB |
| 音频 | 7天 | 自动删除 | 单文件≤50MB |
| 视频成品 | 30天 | 自动删除 | 单文件≤200MB |
| temp中间件 | 合成完成即删 | 立即清理 | — |

**清理机制**

- 定时任务每日扫描 `assets.expires_at`，删除过期文件并清理对应记录
- 任务删除时级联删除其 storage 目录
- 磁盘使用率超过 80% 时触发预警，超过 90% 时优先清理最早的视频成品

**文件访问**

- 内部本地工具默认仅本机访问，文件路径不直接对外暴露
- 下载通过 `/api/v1/tasks/{task_id}/download` 鉴权后流式返回
- 若启用对象存储，URL 使用 1 小时有效的签名链接，不使用公开可读

---

## ⑨ 关键流程衔接与数据流转

本章解决模块间数据传递的衔接细节，明确每个模块的输入从何而来、产物如何流向下游，消除自动流程中的断点。

### 9.1 端到端数据流

```
用户输入：transcript(+keyword/title/author 可选)
   │
   ▼
[A 清洗] cleaned_text
   │
   ▼
[B 改写] script ─────────────┬──────────────┐
   │                         │              │
   ▼(可选)                   ▼              ▼
[C 去重] optimized_script  [D 识别]books   [F 分段]segments
   │                         │              │
   └──► 覆盖 script          ▼              │
                          选主书book_info    │
                             │              │
                             ▼              │
                          [E 配图] images◄──┘(segments供内容插图匹配)
                             │              │
                             └──────┬───────┘
                                    ▼
              用户上传 audio ──► [G 合成] video
```

### 9.2 keyword 的来源与传递（解决矛盾1）

`keyword` 在 A、F 提示词中使用，统一规则如下：

| 场景 | keyword 来源 |
|------|-------------|
| 用户填写了 keyword | 直接使用用户输入，贯穿所有模块 |
| 用户未填写 | 模块 A 完成后，由系统用一次轻量 LLM 调用从 cleaned_text 提取主题词（≤8字），写入 task.keyword |
| 兜底 | 提取失败时 keyword 置空，提示词中该占位符替换为空字符串，不阻断流程 |

`keyword` 作为 task 表字段持久化，后续模块直接读取，不重复提取。

### 9.3 模块 D → E 的衔接（解决矛盾2）

**选主书规则**：D 可能识别出多本书，E 只处理一本。系统按以下优先级选"主书"：

1. 取 confidence 最高的一本
2. confidence 相同时，取 extracted_from 文本出现位置最靠前的
3. 主书的 title/author/category 组装为 E 的 `book_info`

**置信度与自动流程的处理**：

| confidence | 高配版（自动） | 低配版（手动） |
|-----------|---------------|---------------|
| ≥0.8 | 直接使用，继续流程 | 直接使用 |
| 0.5–0.8 | 仍使用主书但标记 `needs_review=true`，任务完成后在详情页提示"书籍信息待确认" | 引导用户人工核对 |
| 全部 <0.5 | E 退化为"通用健康主题"配图（不含具体书名），任务标记 warning | 提示用户手动指定书名 |

说明：高配版不插入阻塞式人工确认，改为"先产出+事后标记"，保证批量流程不中断。

### 9.4 模块 E 提示词变量来源（解决矛盾3）

E 提示词模板中的变量统一由系统在调用前组装：

| 变量 | 来源 |
|------|------|
| {book_title} | 主书 title（D 输出） |
| {health_topic} | 主书 category（D 输出），如"慢性病管理" |
| {content_description} | 对每张内容插图，取其对应 segment 文本经一次 LLM 摘要为≤15字的画面描述 |
| {style_preference} | 任务配置 style_preference，默认"温暖亲切" |

内容插图与 segment 的匹配：按 segment 顺序均匀分配。若内容插图数为 N、segment 数为 M，第 k 张插图取第 `round(k × M / N)` 段作为画面描述来源。

### 9.5 图片时长与音频时长对账（解决矛盾4）

**确定规则：以音频总时长为唯一基准**，图片显示时长由音频反推，E 返回的 `suggested_duration` 仅作权重参考，不作为最终时长。

计算步骤：
1. 读取用户音频总时长 `T_total`
2. 取各图片的 `suggested_duration` 作为权重 `w_i`
3. 归一化分配每张图片实际时长：`d_i = T_total × (w_i / Σw)`
4. 约束修正：单张图片时长限制在 `[2秒, 15秒]`
   - 若修正后总和 ≠ T_total，将差额按比例再分配到未触达边界的图片
   - 若图片总数不足以铺满音频（全部触达 15 秒上限仍短于音频），最后一张图片延长至音频结束
   - 若图片过多导致每张不足 2 秒，按 2 秒下限截断，多余图片丢弃并 `log` 记录丢弃数量

**最终对账保证**：图片轨道总时长 == 音频总时长 == 视频时长，三者严格相等，不允许黑屏或音频截断。

### 9.6 成本预估算法（解决矛盾5）

界面"预估成本"在任务提交前计算，公式如下：

```
预估成本 = LLM预估 + 配图预估

LLM预估 = Σ(各启用LLM模块) 预估token × 单价
  其中预估token：
    - 输入token ≈ transcript字符数 × 1.5（中文约1.5 token/字）
    - 输出token 按模块经验系数：A≈输入×0.9, B≈输入×0.8, C≈输入×1.0,
      D≈固定200, F≈输入×1.0
配图预估 = image_count × 单张配图单价（按 image_provider 取价表）
```

**单价来源**：内置各供应商价目表（随供应商配置切换），价目表可在配置中心维护。

**预估与实际的偏差处理**：
- 预估为上限估计，实际通常更低
- 预估值用于界面展示和 cost_limit 预校验（预估 > cost_limit 时阻断提交）
- 实际成本以 cost_logs 累加为准，运行中超过 cost_limit 立即中断

---

## ⑩ 合规闸门（模块H）

健康养生书 + 50+ 受众 + 带货属于医疗广告高敏感区，仅靠各模块提示词"自觉合规"不足以兜底。本章定义一道独立的系统级合规闸门，置于文案定稿后、视频合成前。

### 10.1 闸门位置

```
[B/C 文案定稿] ──► [H 合规检查] ──► 通过 ──► [F 分段] / [E 配图] / [G 合成]
                       │
                       └─► 不通过 ──► 标记 blocked，回写违规项，阻断后续模块
```

H 为必选模块，自动插入流程，用户不可关闭。

### 10.2 三层检查

**第一层：违禁词库规则匹配（确定性，零成本）**

| 类别 | 示例词 | 处置 |
|------|--------|------|
| 绝对化承诺 | 治愈、根治、100%有效、包好、彻底解决 | 命中即拦截 |
| 医疗功效断言 | 降血压、治疗糖尿病、替代药物 | 命中即拦截 |
| 虚假夸大 | 神效、奇迹、祖传秘方、立竿见影 | 命中即拦截 |
| 诱导/导流 | 加微信、私信我、点击主页、评论区扣 | 命中即拦截 |
| 焦虑恐吓 | 不看后悔、不治会、致命 | 命中告警 |

词库以可维护的配置文件存储，支持增删，命中返回具体词与位置。

**第二层：LLM 语义合规判定**

规则匹配无法覆盖变体和隐含表达，用一次 LLM 调用做语义级判定，输出结构化结果：

```json
{
  "passed": false,
  "violations": [
    {"type": "医疗承诺", "snippet": "原文片段", "severity": "high", "suggestion": "改写建议"}
  ],
  "risk_score": 0.82
}
```

**第三层：人工复核（可选，按风险分流）**

| risk_score | 处置 |
|-----------|------|
| <0.3 | 自动通过 |
| 0.3–0.7 | 通过但标记 `needs_review`，详情页提示人工确认 |
| >0.7 或任一 high 违规 | 阻断，状态置 blocked，必须人工处理后才能继续 |

### 10.3 接口规范

端点：`POST /api/v1/module-h/compliance-check`

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| script | string | 是 | 待检查文案 |
| check_level | enum | 否 | strict/normal，默认 normal |

返回字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| passed | boolean | 是否通过 |
| violations | array | 违规项列表（type/snippet/severity/suggestion） |
| risk_score | float | 综合风险分（0-1） |
| matched_words | array | 命中的违禁词 |
| cost | float | 本次调用成本（元） |

状态码：`200` 完成检查 / `400` 参数错误 / `500` 处理错误

### 10.4 业务规则

- H 失败时任务状态置 `blocked`（新增状态），不进入 failed，区别在于 blocked 可由用户修改文案后重跑
- 闸门只检查文案，配图的合规由 E 的 prompt 约束 + 生成后人工抽检兜底
- 系统对带货内容仅做技术性合规辅助，不构成法律/医疗合规保证，最终发布责任由用户承担（需在用户协议中明示）

---

## ⑪ 错误处理、重试与批量规格

### 11.1 模块超时与重试

| 模块 | 单次超时 | 最大重试 | 可重试错误 | 不可重试错误 |
|------|---------|---------|-----------|-------------|
| A/B/C/D/F | 30秒 | 2次 | 超时、5xx、限流429 | 参数错误400、Key无效401 |
| E 配图 | 60秒/张 | 2次 | 超时、5xx、限流 | 内容被拒、余额不足 |
| H 合规 | 30秒 | 2次 | 超时、5xx | 参数错误 |
| G 合成 | 300秒 | 1次 | 渲染中断、临时IO | 输入缺失、格式错误 |

重试策略：指数退避（2s、4s），重试记入 `module_results.retry_count`。

### 11.2 失败分级与处置

| 失败类型 | 处置 | 任务最终状态 |
|---------|------|-------------|
| 必选模块（A/B/E/F/G/H）重试耗尽 | 中断，保留已完成产物 | failed |
| 可选模块（C/D）失败 | 跳过该模块，流程继续 | completed（带 warning） |
| 合规拦截（H 不通过） | 阻断后续，可改文案重跑 | blocked |
| 超时（超 time_limit） | 中断当前模块 | failed（error_code=TIMEOUT） |
| 超成本（超 cost_limit） | 中断 | failed（error_code=COST_EXCEEDED） |
| 每日成本上限触顶 | 拒绝新任务 | 任务不创建，返回 E2003 |

**断点重试**：失败任务重跑时，已 success 的 module_results 直接复用，不重复扣费，仅从失败模块继续。

### 11.3 错误码补充

| 错误码 | 含义 | 处理建议 |
|--------|------|----------|
| E2003 | 每日成本上限已达 | 提示次日或调高上限 |
| E3003 | 音频文件损坏/无法解析 | 提示重新生成上传 |
| E4001 | 模块处理超时 | 自动重试，耗尽后提示 |
| E4002 | 合规检查未通过 | 展示违规项，引导改文案 |
| E4003 | 配图被供应商内容拒绝 | 提示调整书籍/关键词 |
| E5001 | 视频渲染失败 | 查看日志，重试 |
| E5002 | 磁盘空间不足 | 提示清理或扩容 |

### 11.4 批量处理规格

针对场景 B 的 ZIP 批量上传：

**ZIP 内容约定**

```
batch.zip
  ├── 001_关键词A.txt    （文件名：序号_关键词，关键词可选）
  ├── 002_关键词B.txt
  └── ...
```

- 每个 `.txt` 为一条逐字稿，UTF-8 编码
- 文件名下划线后部分作为该任务 keyword（可选）
- 单批次最多 100 个文件，单文件遵循 transcript 长度限制

**批量处理规则**

| 项目 | 规则 |
|------|------|
| 批次记录 | 创建 batch_id，每个 txt 生成一条独立 task，关联 batch_id |
| 并发 | 受 configs.concurrency 限制，排队执行 |
| 部分失败 | 单任务失败不影响其他任务，批次整体标记 partial_success |
| 进度 | 批次详情页展示 N 个任务的成功/失败/处理中统计 |
| 成本 | 批次预估 = 各任务预估之和，超每日上限时拒绝并提示 |
| 下载 | 批次完成后支持打包下载全部成功视频（ZIP） |

**批次状态**：`queued → processing → completed / partial_success / failed`

---

## ⑫ 系统鉴权与安全

### 12.1 系统访问鉴权

即便定位为单用户内部工具，只要服务监听端口就存在被访问的风险，必须有访问控制。

| 部署形态 | 鉴权方式 |
|---------|---------|
| 仅本机使用 | 服务绑定 `127.0.0.1`，不监听 `0.0.0.0`，不对外暴露 |
| 局域网/远程访问 | 启用访问令牌：首次启动生成随机 token，所有 `/api/v1/*` 请求需带 `Authorization: Bearer <token>` |
| 公网部署（不推荐） | 强制 HTTPS + token + IP 白名单，并在文档中明确风险 |

- 鉴权失败返回 `401`
- 健康检查 `/health` 端点可豁免鉴权
- 默认配置为"仅本机 + 不暴露"，最安全

### 12.2 密钥安全

- LLM / 绘图 API Key 使用对称加密（Fernet/AES-GCM）存储于 `configs` 表，密钥不落明文
- 加密主密钥从环境变量读取，不写入代码库和数据库
- 日志、错误信息、接口返回中一律不输出明文密钥
- 前端展示掩码（`sk-****1234`），更新时整体替换，不回显原值

### 12.3 输入与传输安全

| 项目 | 措施 |
|------|------|
| 文本输入 | 长度与字符校验，防止超大 payload |
| 文件上传 | 校验 MIME 与扩展名、大小上限、魔数检测，拒绝可执行文件 |
| ZIP 解压 | 防 Zip Slip（路径穿越）、解压炸弹（限制总大小与文件数） |
| 外部内容 | LLM/绘图返回内容视为不可信数据，不直接执行，落库前转义 |
| 传输 | 公网部署强制 HTTPS |

### 12.4 数据隐私

- 逐字稿、文案、生成产物仅存于本地，不上传至第三方（除用户配置的 LLM/绘图供应商外）
- 调用 LLM/绘图 API 即向对应供应商传输文案/提示词，需在用户协议中告知
- 不收集与上传用户行为数据到本系统之外

---

## ⑬ 测试与验收标准

### 13.1 测试范围

| 测试类型 | 覆盖对象 | 工具/方式 |
|---------|---------|----------|
| 单元测试 | 各模块入参校验、字幕对齐算法、成本预估算法、时长对账 | pytest |
| 集成测试 | A→B→C→D→E→F→H→G 全链路、断点重试、批量 | pytest + mock API |
| 接口测试 | 全部 REST 端点的正常/异常路径 | pytest + httpx |
| 渲染测试 | 模块 G 实际产出 MP4 校验 | FFprobe 校验输出 |
| 合规测试 | 违禁词库命中、LLM 语义判定准召 | 标注样本集回归 |
| 性能测试 | 单条端到端时长、并发吞吐 | locust / 自定义脚本 |
| 安全测试 | 鉴权、密钥不泄露、Zip Slip、文件类型 | 手工 + 自动化用例 |

### 13.2 模块级验收标准

| 模块 | 验收标准 |
|------|---------|
| A 清洗 | 水印/营销话术删除率 100%（样本集）；核心事实零篡改（人工抽检） |
| B 改写 | 输出 200-400 字；含开头钩子与 CTA；通过 H 合规 |
| C 去重 | similarity 落在 0.3-0.7；核心钩子保留 |
| D 识别 | 标注集书名识别准确率 ≥90%；输出严格 JSON |
| E 配图 | 生成 3-6 张，均为 1080x1920；含 1 封面 + 1 CTA |
| F 分段 | 每段 24-28 秒；严格 JSON；不改写原文 |
| H 合规 | 高危违禁词召回率 ≥98%；误拦截率 ≤5% |
| G 合成 | 输出 1080x1920、≥25fps；音画同步偏差 ≤0.5s；时长=音频时长 |

### 13.3 系统级验收标准

| 指标 | 目标 | 验收方法 |
|------|------|---------|
| 单条端到端时长 | ≤15 分钟（不含用户 TTS 与上传） | 50 条样本统计 P95 |
| 单条成本 | ≤1.0 元 | cost_logs 统计均值与 P95 |
| 处理成功率 | ≥98% | 100 条批量统计 |
| 系统可用性 | ≥99.5% | 30 天监控 |
| 字幕对齐准确性 | 主观一致，无明显错位 | 人工抽检 20 条 |
| 接口 P95 响应 | ≤2 秒（不含异步处理） | 接口压测 |

### 13.4 验收用例样例

| 用例 | 输入 | 预期 |
|------|------|------|
| TC-01 正常全流程 | 1500字逐字稿，全模块 | 产出合规 MP4，成本<1元，<15分钟 |
| TC-02 transcript 过短 | 5字文本 | 返回 E1001，任务不创建 |
| TC-03 成本超限 | 预估>cost_limit | 提交被阻断，提示调整 |
| TC-04 合规拦截 | 含"根治糖尿病"文案 | H 拦截，状态 blocked，列出违规项 |
| TC-05 配图失败重试 | mock E 首次 5xx | 自动重试成功，不重复扣费 |
| TC-06 批量部分失败 | 10条含1条超长 | 9成功1失败，批次 partial_success |
| TC-07 多本书识别 | 文案含2本书 | 选 confidence 最高者为主书配图 |
| TC-08 音频超长对账 | 音频3分钟，图片5张 | 图片时长按权重铺满3分钟，无黑屏 |
| TC-09 未授权访问 | 无 token 请求 API | 返回 401 |
| TC-10 断点续跑 | A-E 成功后 G 失败重跑 | 复用 A-E 结果，仅重跑 G |

---

## ⑭ 风险与应对

### 14.1 合规风险（最高优先级）

| 风险 | 影响 | 应对 |
|------|------|------|
| 健康/医疗夸大表达导致违规 | 视频下架、账号封禁 | 模块 H 三层合规闸门；违禁词库持续维护 |
| 图书带货资质不全 | 商品被下架、店铺扣分 | 用户协议明示资质自负；产品不代办资质 |
| 搬运他人内容版权争议 | 投诉、限流 | 模块 A/C 改写降低相似度；提示用户对原创性负责 |
| 平台规则变动 | 既有内容批量违规 | 违禁词库与提示词可热更新；关注平台政策 |

**核心立场**：系统提供技术性合规辅助，不构成法律或医疗合规保证，最终发布与商品责任由用户承担。

### 14.2 技术风险

| 风险 | 影响 | 应对 |
|------|------|------|
| LLM/绘图 API 不稳定或涨价 | 流程中断、成本上升 | 多供应商可切换；重试机制；成本上限保护 |
| 视频渲染失败或卡顿 | 产出延迟 | 单独超时与重试；FFmpeg 参数兜底；失败保留中间产物 |
| 字幕按字符比例对齐偏差 | 字幕与语音错位 | 标注为近似对齐；提供导出字幕供用户在剪映微调 |
| 配图质量不稳定 | 成品观感差 | 允许重新生成单张；人工抽检 |
| 磁盘占满 | 任务失败 | 生命周期清理 + 阈值预警 |

### 14.3 业务与产品风险

| 风险 | 影响 | 应对 |
|------|------|------|
| 成本预估偏差大 | 用户信任下降 | 预估取上限值；实际成本透明展示 |
| 字幕对齐为近似方案 | 用户预期落差 | 文档与界面明确说明，提供微调出口 |
| 新手上手难 | 完成率低 | 低配版 7 步引导 + 一键复制提示词 |
| 单用户定位限制规模 | 商业化受限 | V1 聚焦内部工具验证，多用户列入演进 |

### 14.4 关键假设与依赖

- 假设用户已具备视频号小店账号与图书类目资质
- 假设用户能自行用剪映等工具完成 TTS 配音
- 依赖第三方 LLM 与绘图 API 的可用性与价格稳定性
- 依赖本机或服务器具备 FFmpeg 运行环境

---

## ⑮ 里程碑与排期

### 15.1 开发阶段划分

| 阶段 | 目标 | 主要交付 | 预估周期 |
|------|------|---------|----------|
| M0 准备 | 环境与基建 | 项目脚手架、数据库表、配置中心、API Key 加密、鉴权 | 1 周 |
| M1 文案链路 | A/B/C/D/H 跑通 | 5 个 LLM 模块 + 合规闸门 + 成本记录 | 2 周 |
| M2 媒资链路 | E/F/G 跑通 | 配图、分段、视频合成、字幕对齐、时长对账 | 2 周 |
| M3 编排与任务 | 全链路自动化 | 任务管理、Celery 队列、断点重试、状态机 | 1.5 周 |
| M4 前端与交互 | 可用界面 | 低配版引导、高配版创建、任务详情、音频上传、配置 | 2 周 |
| M5 批量与稳定 | 规模能力 | ZIP 批量、并发控制、文件生命周期、监控告警 | 1.5 周 |
| M6 测试与验收 | 达标上线 | 测试用例、性能压测、安全测试、验收 | 1.5 周 |

合计约 11.5 周（单团队估算，可并行压缩）。

### 15.2 MVP 边界（最小可用）

为尽早验证，MVP 优先交付端到端单条链路：

**MVP 包含**：A、B、E、F、G、H + 高配版单任务创建 + 音频上传 + 下载 + 配置中心 + 本机鉴权。

**MVP 暂不含**：C 去重、低配版引导页、ZIP 批量、对象存储、多供应商动态切换（先固定 1 个 LLM + 1 个绘图供应商）。

MVP 目标：单条逐字稿 → 合规 MP4，成本 <1 元，<15 分钟。

### 15.3 演进方向（V1 之后）

- 多用户与权限体系
- 多供应商智能选优（按价格/质量自动路由）
- 配图风格模板库与品牌化
- 字幕逐句精确对齐（引入轻量 ASR 对齐替代字符比例）
- 一键发布对接视频号（视平台开放能力而定）
- 数据看板：转化、成本、产出趋势

### 15.4 关键路径与风险点

- **关键路径**：M1 文案链路 → M2 媒资链路 → M3 编排，三者串行依赖，任一延期影响整体
- **最大不确定项**：模块 G 视频合成的渲染性能与字幕对齐效果，建议 M2 早期做技术预研验证
- **外部依赖**：LLM/绘图 API 的稳定性与价格，建议 M0 阶段完成选型与成本实测

---

## 附录：版本记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V1.0 | 2026-06-04 | 初始版本：背景、价值、场景、7 模块功能、字段规则、交互、技术架构 |
| V1.1 | 2026-06-04 | 补充：数据模型与存储、流程衔接（解决字段矛盾）、合规闸门（模块H）、错误处理与批量、鉴权安全、测试验收、风险、里程碑 |
























