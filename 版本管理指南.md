# Git 版本管理指南

## 📋 查看所有版本

```bash
# 查看所有提交历史
git log --oneline --graph

# 查看所有版本tag
git tag -l

# 查看最近5个提交
git log --oneline -5
```

## ⏮️ 回退到指定版本

### 方式1: 回退到tag版本（推荐）
```bash
# 查看可用的版本
git tag -l

# 回退到数据库防护版本
git checkout v1.2.0-db-protection

# 或回退到之前的某个版本
git checkout <tag-name>
```

### 方式2: 回退到指定提交
```bash
# 查看提交历史
git log --oneline -10

# 回退到指定提交（commit hash前7位）
git checkout f0c82cf

# 回退到上一个版本
git checkout HEAD~1
```

### 方式3: 创建新分支保留当前状态
```bash
# 先创建备份分支（保留当前状态）
git branch backup-20260627

# 然后回退到旧版本
git checkout v1.2.0-db-protection
```

## 🔄 恢复到最新版本

```bash
# 回到主分支最新版本
git checkout master

# 或
git checkout main
```

## 🏷️ 打版本标签（开发用）

```bash
# 打轻量标签
git tag v1.3.0

# 打带说明的标签（推荐）
git tag -a v1.3.0 -m "版本说明"

# 推送标签到远程
git push origin v1.3.0

# 推送所有标签
git push origin --tags
```

## ⚠️ 注意事项

1. **回退前先提交当前修改**
   ```bash
   git add .
   git commit -m "保存当前修改"
   ```

2. **数据库版本兼容**
   - 回退代码版本时，数据库可能不兼容
   - 建议同时备份数据库文件

3. **打包应用前打tag**
   - 每次打包桌面应用前先打tag
   - 便于追踪哪个版本在生产环境

## 📝 版本命名规范

```
v<主版本>.<次版本>.<修订号>-<标识>

例如：
v1.0.0          - 初始版本
v1.1.0          - 新增功能
v1.1.1          - 修复bug
v1.2.0-db-protection - 数据库防护版本
```

## 🚀 快速命令参考

```bash
# 查看当前版本
git describe --tags

# 查看当前分支
git branch

# 查看修改状态
git status

# 放弃所有本地修改（危险！）
git reset --hard HEAD

# 查看某个文件的历史版本
git log --oneline -- backend/app/main.py
```
