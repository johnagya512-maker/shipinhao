# 数据库防护方案

> 防止数据库损坏和数据丢失

## 🛡️ 已实施的防护措施

### 1. 启动时健康检查
- ✅ 自动检测数据库完整性（SQLite PRAGMA integrity_check）
- ✅ 如果检测到损坏，拒绝启动并提示从备份恢复
- ✅ 防止损坏数据库被进一步破坏

### 2. 自动备份机制
- ✅ **每次应用启动时自动备份**
- ✅ 备份文件命名：`app.db.backup_YYYYMMDD_HHMMSS`
- ✅ 保留最近7个备份，自动清理旧备份
- ✅ 只备份完整性检查通过的数据库

### 3. 优雅关闭
- ✅ 应用关闭时执行WAL checkpoint
- ✅ 确保WAL文件数据写入主数据库
- ✅ 安全关闭所有数据库连接

## 📍 备份位置

### 开发环境
```
D:\chennigongzuoshi\shipinhao\backend\
  └── app.db.backup_YYYYMMDD_HHMMSS
```

### 生产环境（桌面应用）
```
C:\Users\<用户名>\AppData\Roaming\shipinhao-desktop\data\
  ├── app.db                              # 主数据库
  ├── app.db.backup_20260627_103841      # 自动备份1
  ├── app.db.backup_20260627_115000      # 自动备份2
  └── app.db.backup_20260627_120000      # 自动备份3...
```

## 🔄 数据恢复流程

### 自动恢复（推荐）
1. 关闭应用
2. 运行：`修复数据库.bat`
3. 脚本会自动选择最新的备份恢复

### 手动恢复
```bash
# 1. 关闭应用
taskkill /F /IM "视频号图书带货AI.exe"

# 2. 进入数据目录
cd %APPDATA%\shipinhao-desktop\data

# 3. 备份损坏的数据库
move app.db app.db.corrupted

# 4. 恢复备份（选择最新的）
copy app.db.backup_20260627_HHMMSS app.db

# 5. 删除WAL文件
del app.db-shm
del app.db-wal

# 6. 重启应用
```

## ⚠️ 数据库损坏的常见原因

1. **强制关闭应用**
   - 任务管理器强制结束进程
   - 系统崩溃/断电
   - 防护：使用应用内的关闭按钮，等待优雅退出

2. **磁盘空间不足**
   - WAL文件无法写入
   - 防护：定期清理磁盘，保持至少1GB空闲空间

3. **并发写入冲突**
   - 多个进程同时访问数据库
   - 防护：确保只运行一个应用实例

4. **硬件故障**
   - 磁盘坏道、内存错误
   - 防护：定期备份到其他位置

## 📊 最佳实践

### 日常使用
- ✅ 使用应用内的"关闭"按钮退出
- ✅ 不要在任务运行时强制关闭
- ✅ 定期检查磁盘空间
- ✅ 关闭前等待所有任务完成

### 开发/调试
- ✅ 使用 `Ctrl+C` 停止后端（触发优雅关闭）
- ✅ 不要直接杀进程
- ✅ 修改代码前先备份数据库

### 重要操作前
- ✅ 手动备份数据库文件
- ✅ 导出重要任务数据
- ✅ 记录当前配置（API密钥等）

## 🔧 紧急修复工具

### 修复数据库.bat
自动化修复脚本，已包含：
- 关闭应用
- 检测备份
- 恢复数据
- 重启应用

### check_presets.py
检查配图预设是否完整

### update_presets_prod.py
恢复丢失的配图预设到生产库

## 📝 监控检查清单

定期执行以下检查：

- [ ] 备份文件是否正常生成（每次启动）
- [ ] 磁盘剩余空间是否充足（>1GB）
- [ ] 应用日志是否有数据库错误
- [ ] 数据库文件大小是否异常增长

## 🚀 版本控制

所有改动已提交到git，打上版本tag：
```bash
git tag -a v1.2.0-db-protection -m "添加数据库防护机制"
```

可随时回退到稳定版本。

---

**最后更新**: 2026-06-27
**防护版本**: v1.2.0
