# ── 依赖与构建产物 ──
node_modules/
dist/
build/
*.tsbuildinfo

# ── Python ──
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── 本地数据与运行产物（含加密 Key，绝不入库）──
app.db
*.sqlite
*.sqlite3
storage/

# ── 密钥与环境 ──
.env
.env.*
*.key

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 临时文档（按需取消注释保留）──
# 新建 Microsoft Word 文档.docx

# 运行日志（后端文件日志，不入库）
backend/logs/
*.log

# 桌面端打包产物
desktop/release/
