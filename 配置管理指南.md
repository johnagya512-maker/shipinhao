# 配置管理指南

> 避免重装应用后需要重新填写API密钥

## 🔑 API密钥保存位置

### 生产环境（桌面应用）
```
C:\Users\<用户名>\AppData\Roaming\shipinhao-desktop\data\app.db
```

### 开发环境
```
D:\chennigongzuoshi\shipinhao\backend\app.db
```

密钥以**加密形式**保存在configs表中。

## 📦 配置导出导入工具

### 导出配置（备份）

```bash
cd backend
python export_config.py export

# 或指定文件名
python export_config.py export 我的配置_2026-06-27.json
```

**导出内容包括**：
- ✅ 所有API密钥（加密状态）
- ✅ 配图预设（5个预设）
- ✅ TTS配置
- ✅ 并发设置
- ✅ 所有其他配置项

**导出文件示例**：
```json
{
  "llm_provider": "deepseek",
  "llm_model": "deepseek-chat",
  "llm_api_key_enc": "68657820656e636f646564...",
  "image_presets": [
    {
      "name": "豆包 Seedream（兔子）",
      "model": "doubao-seedream-4-5-251128",
      "base_url": "https://api.tu-zi.com/v1/images/generations",
      "unit_price": 0.25
    }
  ],
  ...
}
```

### 导入配置（恢复）

```bash
cd backend
python export_config.py import config_backup_20260627_120000.json

# 导入到指定数据库
python export_config.py import config.json "C:\Users\...\app.db"
```

## 🔄 使用场景

### 场景1: 重装应用前备份

```bash
# 1. 导出配置
cd D:\chennigongzuoshi\shipinhao\backend
python export_config.py export

# 2. 保存导出的JSON文件到安全位置
# 例如: D:\备份\config_backup_20260627.json

# 3. 重装应用

# 4. 恢复配置
python export_config.py import D:\备份\config_backup_20260627.json
```

### 场景2: 开发环境 → 生产环境

```bash
# 1. 导出开发环境配置
cd backend
python export_config.py export dev_config.json

# 2. 导入到生产数据库
python export_config.py import dev_config.json "C:\Users\Administrator\AppData\Roaming\shipinhao-desktop\data\app.db"
```

### 场景3: 团队共享配置

```bash
# 1. 导出配置模板（删除敏感信息）
python export_config.py export team_template.json

# 2. 手动编辑JSON，替换实际的API密钥

# 3. 团队成员导入
python export_config.py import team_template.json
```

## ⚠️ 重要注意事项

### 1. 加密密钥依赖

导出的API密钥是**加密状态**，只能在**相同的APP_ENCRYPTION_KEY**下使用。

- ✅ 相同环境恢复：完全没问题
- ❌ 不同环境恢复：需要相同的加密密钥
- ❌ 明文导出：不支持（安全考虑）

**解决方案**：
```bash
# 方式1: 设置相同的加密密钥
set APP_ENCRYPTION_KEY=<你的密钥>

# 方式2: 导入后重新填写API密钥（推荐）
```

### 2. 文件安全

配置文件包含加密的API密钥，请：
- 🔒 不要提交到git仓库
- 🔒 不要分享给不信任的人
- 🔒 保存在安全的位置（加密磁盘）

### 3. 配置版本兼容

不同版本的应用可能有不同的配置字段：
- 旧版本导入到新版本：缺失的字段会使用默认值
- 新版本导入到旧版本：多余的字段会被忽略

## 🎯 最佳实践

### 定期备份配置

在以下时机导出配置：
- ✅ 修改重要配置后（API密钥、预设）
- ✅ 重装应用前
- ✅ 系统升级前
- ✅ 每周一次（自动化）

**自动备份脚本**：
```batch
@echo off
cd /d D:\chennigongzuoshi\shipinhao\backend
python export_config.py export D:\备份\配置\config_%date:~0,10%.json
echo 配置已备份到 D:\备份\配置\
```

### 配置文件命名规范

```
config_backup_YYYYMMDD_HHMMSS.json        # 时间戳
config_生产环境_v1.2.0.json                 # 版本标记  
config_团队共享_模板.json                    # 用途说明
```

### Git管理（仅模板）

```bash
# .gitignore 中添加
config_backup_*.json
config_生产*.json

# 只提交模板
git add config_template.json
```

## 🚀 快速命令

```bash
# 导出当前配置
python backend/export_config.py export

# 恢复配置
python backend/export_config.py import config_backup_<日期>.json

# 查看配置文件
cat config_backup_<日期>.json | python -m json.tool
```

---

## 🔐 安全提示

1. **定期更换API密钥**
   - 建议每3-6个月更换一次
   - 更换后重新导出配置

2. **配置文件加密**
   - 可以用7-Zip等工具加密压缩配置文件
   - 密码单独保存

3. **备份存储**
   - 本地备份：外置硬盘、加密U盘
   - 云端备份：加密后上传（OneDrive/百度网盘）
   - 多地备份：至少2个独立位置

---

**最后更新**: 2026-06-27
